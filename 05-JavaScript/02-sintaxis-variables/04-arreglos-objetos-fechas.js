// arreglo
const array = ['hola', 1, 2, 3];
const lista2 = new Array('hola', 1, 2, 3);

// Objeto
const movil = {
  altura: 10,
  anchura: 5,
  marca: 'Xiaomi',
  isWhite: false,
  contactos: ['Gorka', 'Martin'],
  tarjeta: {
    marca: 'Sandisk',
    almacenamiento: 32,
  },
};

movil.anio = 2019;
console.log(movil.anio);

movil.marca = 'Samsung';

// Fecha
const ahora = new Date();
